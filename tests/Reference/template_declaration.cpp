// Test template declarations (forward declarations)
template<typename T>
T max(T a, T b);

template<typename T>
T min(T a, T b);

template<typename T, typename U>
T add(T a, U b);

template<int N>
int multiply_by_n(int x);

template<class T>
T identity(T value);

int main() {
    return 0;
}

