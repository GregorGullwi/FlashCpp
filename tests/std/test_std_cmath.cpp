// Test for <cmath> standard header compilation
// Tests that the glibc math header macros (__MATHDECL_ALIAS, __MATHCALL_VEC, etc.)
// are correctly expanded through nested macro chains with empty arguments
#include <cmath>

int main() {
    return 0;
}
