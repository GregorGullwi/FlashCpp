int main() {
    int a = 10;
    int b = 3;
    int sum = a + b;      // Should use typed Add
    int diff = a - b;     // Should use typed Subtract
    int prod = a * b;     // Should use typed Multiply
    return sum + diff + prod;
}
